include(cc_library)

cc_library(
  NAME 
    model
  HDRS
    causal_lm.h
    rec_causal_lm.h
    causal_vlm.h
    dit_model.h
    model_args.h
    model_input_params.h
    $<$<BOOL:${USE_TORCH_DELEGATE}>:ep_model.h>
  SRCS
    $<$<BOOL:${USE_TORCH_DELEGATE}>:ep_model.cpp>
  DEPS
    :common
    :common_layers
    :kv_cache
    :parallel_state
    :processors
    :chat_template
    glog::glog
    torch
    torch_python
    $<$<BOOL:${USE_NPU}>:platform_npu>
    $<$<BOOL:${USE_TORCH_DELEGATE}>:TorchDelegate::torch_delegate_backend>
)
